# SQLite (default - works locally without extra setup)
DATABASE_URL="file:./dev.db"

# Auth — change in production (min 32 characters)
AUTH_SECRET="change-this-to-a-long-random-secret-key-32chars"

# Default admin password for first seed (optional)
ADMIN_PASSWORD="priti@2025"

# For production (PostgreSQL on Neon, Supabase, Railway, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/priti_enterprises?schema=public"
